<Window x:Class="ChatRoom.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatRoom.Client"
        mc:Ignorable="d"
        Title="ChatRoom Client" Height="650" Width="450" 
        Background="#F0F2F5" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate x:Key="MessageTemplate">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="MessageBubble" Grid.Column="1" Padding="10" CornerRadius="10" MaxWidth="250" Margin="5">
                    <StackPanel>
                        <TextBlock x:Name="SenderName" Text="{Binding Sender}" FontSize="10" Foreground="Gray" Margin="0,0,0,2"/>
                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="14"/>
                        <TextBlock Text="{Binding Time, StringFormat=HH:mm}" FontSize="9" Foreground="#888888" HorizontalAlignment="Right" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsMe}" Value="True">
                    <Setter TargetName="MessageBubble" Property="Background" Value="#DCF8C6"/>
                    <Setter TargetName="MessageBubble" Property="HorizontalAlignment" Value="Right"/>
                    <Setter TargetName="SenderName" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding IsMe}" Value="False">
                    <Setter TargetName="MessageBubble" Property="Background" Value="White"/>
                    <Setter TargetName="MessageBubble" Property="HorizontalAlignment" Value="Left"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#DDDDDD" BorderThickness="1" Background="White" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="CONNECTION SETTINGS" FontWeight="Bold" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtIP" Grid.Column="0" Text="192.168.1.9" Margin="0,0,5,0" Padding="5" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnConnect" Grid.Column="1" Content="CONNECT" Background="#2196F3" Foreground="White" Click="btnConnect_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#0078D7" Padding="15">
            <Grid>
                <TextBlock Text="LAN CHAT ROOM" Foreground="White" FontWeight="Bold" FontSize="18" VerticalAlignment="Center"/>
                <Button x:Name="btnHistory" Content="📅 History" HorizontalAlignment="Right" Background="Transparent" BorderBrush="White" Foreground="White" Click="btnHistory_Click"/>
            </Grid>
        </Border>

        <ListBox x:Name="lbChat" Grid.Row="2" Background="Transparent" BorderThickness="0" 
                 ItemTemplate="{StaticResource MessageTemplate}" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Padding="10">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <Border Grid.Row="3" Background="White" Padding="10" BorderBrush="#E5E5E5" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnFile" Grid.Column="0" Content="📎" Width="35" Height="35" Background="Transparent" BorderThickness="0" Foreground="#555" FontSize="18" Click="btnFile_Click" ToolTip="Send File"/>
                <Button x:Name="btnEmoji" Grid.Column="1" Content="😊" Width="35" Height="35" Background="Transparent" BorderThickness="0" Foreground="#555" FontSize="18" Click="btnEmoji_Click" ToolTip="Send Emoji"/>

                <TextBox x:Name="txtMessage" Grid.Column="2" Margin="5,0" Padding="10,8" VerticalContentAlignment="Center" BorderThickness="1" BorderBrush="#DDD" KeyDown="txtMessage_KeyDown">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="15"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <Button x:Name="btnSend" Grid.Column="3" Content="➤" Width="40" Height="35" Background="#0078D7" Foreground="White" BorderThickness="0" FontSize="16" Click="btnSend_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="15"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>